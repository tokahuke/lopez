/// A prelude of basic analyses. This is *not* imported by default, although
/// it is, by definition, a good default.

import "super.seo";
import "super.og";
import "super.ignore-tracking";
import "super.bad-extensions";

import "super.frontiers.internet-archives";
